# Core
numpy>=1.24,<3
pandas>=2.0,<3
scipy>=1.10,<2
scikit-learn>=1.3,<2

# Visualization / App
plotly>=5.20,<6
streamlit>=1.36,<2
matplotlib>=3.8,<4

# Utilities
tqdm>=4.66,<5
packaging>=24.0,<25

# NOTE:
# We intentionally do NOT pin torch here because GPU/CPU wheels vary by CUDA/OS.
# Install PyTorch separately for your environment:
#   CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   CPU-only : pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
